# Required for the Guardian editorial feed. Keep this file empty of real keys.
# Create backend/.env.local for local development and set the key in your host's
# environment-variable dashboard for deployments.
GUARDIAN_API_KEY=
