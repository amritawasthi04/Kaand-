name: application
description: "KAAND — Stay Connected. Stay Informed."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dotlottie_flutter: ^0.1.4

  # UX & Design Enhancements
  google_fonts: ^8.1.0
  lottie: ^3.5.1
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/animations/
    - assets/logo/
