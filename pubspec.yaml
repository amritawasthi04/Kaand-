name: application
description: "KAAND — Stay Connected. Stay Informed."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  provider: ^6.1.2

  # Networking
  dio: ^5.5.0+1

  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Image & Asset handling
  cached_network_image: ^3.4.1
  flutter_svg: ^2.0.10+1

  # UX & Design Enhancements
  flutter_animate: ^4.5.0
  google_fonts: ^6.2.0
  lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/animations/
    - assets/logo/
