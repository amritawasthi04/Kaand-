name: Repository Safety Notice
on:
  workflow_dispatch:

jobs:
  safety-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm protected repository workflow
        run: |
          echo "This workflow intentionally performs no repository mutations."
          fi
